# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Secrets / env files (never commit) — keep .env.example tracked
.env
.env.*
!.env.example
*.backup
*.backup.*

# Private keys / agent credentials
secrets/
*.pem
*.key

# Runtime state (sessions, access-request cache)
.data/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Local backups / scratch (never publish)
backups/
*.bak.*
XAI.md
